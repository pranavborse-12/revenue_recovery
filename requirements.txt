# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Configuration ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Razorpay ---
razorpay==1.4.2
# The razorpay SDK still does `import pkg_resources` internally.
# pkg_resources was removed from setuptools' default install in newer
# versions (it's deprecated upstream). Pinning setuptools<81 keeps
# pkg_resources available until Razorpay updates their SDK.
setuptools<81

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Async task queue (Phase 2) ---
celery==5.4.0
redis==5.2.1